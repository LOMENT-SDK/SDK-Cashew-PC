<Page x:Class="WpfCashew.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfCashew"
      xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="ChatPage" DataContext="{Binding LomentAccount, Mode=OneWay, Source={StaticResource Locator}}">


    <Grid>
        <Button Content=" SETTINGS"  HorizontalAlignment="Right" VerticalAlignment="Top" Command="{Binding GoToSettings}"/>
        <TabControl ItemsSource="{Binding CashewAccounts}" SelectedItem="{Binding SelectedCashewAccount, Mode=TwoWay}" TabStripPlacement="Left" SelectedIndex="0" Margin="0,30,0,0" >
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding CashewAccount.UserName}"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <local:ChatControl IsEnabled="{Binding IsRunning}" />                
                </DataTemplate>
            </TabControl.ContentTemplate>

        </TabControl>

    </Grid>
</Page>
